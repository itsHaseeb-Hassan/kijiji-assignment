import React from 'react'

const Button = ({text}) => {
  return (
    <div>
        <button className="bg-white font-larsseit p-4 mt-4 w-[100px] rounded-md text-postbtn text-[16px] font-medium">{text}</button>
    </div>
  )
}

export default Button